"use client"
import { FC } from 'react';

interface BannerColProps { }

const BannerCol: FC<BannerColProps> = ({ }) => {
    return (
        <div className="hidden md:block w-[20%] flex-shrink-0 pl-6 bg-white">
            {/* Placeholder for side-column banners */}
        </div>
    );
};

export default BannerCol;
 

