import React, { FC } from 'react';
import Directory from '../../(main)/directory/page';

interface LoggedDirectoryProps {
  
}

const LoggedDirectory: FC<LoggedDirectoryProps> = ({ }) => {
  return (
    <Directory />
  );
};

export default LoggedDirectory;